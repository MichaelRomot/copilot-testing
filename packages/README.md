# packages

This folder contains workspace packages:

- `client` – React client app (Vite + TypeScript)
- `server` – NestJS server app (TypeScript)
- `mcp` – placeholder package for MCP-related code

CI test: add this line to trigger Bun CI workflow for PR validation.
